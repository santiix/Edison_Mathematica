<html lang="en">
<head>
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-WQGMR9BRDM"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-WQGMR9BRDM');
</script>
  <meta charset="UTF-8">
  <title>Time Calculator Minutes - Add or Subtract Days, Hours, Minutes, Seconds</title>
  <meta name="description" content="Use this free time calculator to add or subtract time in days, hours, minutes, and seconds. See totals in all units with smart formatting." />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body { font-family: 'Segoe UI', sans-serif; background: #f9f9f9; color: #333; padding: 2rem; margin: 0; }
    .container { max-width: 700px; margin: auto; background: #fff; padding: 2rem; border-radius: 1rem; box-shadow: 0 10px 25px rgba(0,0,0,0.05); }
    h1 { text-align: center; color: #264653; }
    .section { margin-top: 2rem; }
    label { font-weight: 600; margin-top: 1rem; display: block; }
    input { width: 100%; padding: 0.5rem; margin-top: 0.3rem; border-radius: 0.5rem; border: 1px solid #ccc; }
    select, button { width: 100%; margin-top: 1rem; padding: 0.75rem; border-radius: 0.5rem; font-weight: bold; border: none; }
    button { background: #2a9d8f; color: white; cursor: pointer; }
    button:hover { background: #21867a; }
    .result { margin-top: 2rem; background: #f1f8f7; padding: 1rem; border-radius: 0.5rem; }
    .back-link { text-align: center; margin-top: 3rem; }
    .back-link a { display: inline-block; padding: 0.6rem 1.2rem; background-color: #e0f2f1; color: #00796b; border-radius: 1.5rem; text-decoration: none; font-weight: 600; box-shadow: 0 2px 6px rgba(0,0,0,0.05); }
  </style>
</head>
<body>
  <div class="container">
    <h1>⏱ Time Calculator (Minutes)</h1>
    <p style="text-align:center">Add or subtract two time values. Supports days, hours, minutes, and seconds. Blank fields are treated as 0.</p>

    <div class="section">
      <h2>⏳ Time 1</h2>
      <label>Days</label><input id="d1" type="number" min="0">
      <label>Hours</label><input id="h1" type="number" min="0">
      <label>Minutes</label><input id="m1" type="number" min="0">
      <label>Seconds</label><input id="s1" type="number" min="0">
    </div>

    <div class="section">
      <h2>➕➖ Time 2</h2>
      <label>Days</label><input id="d2" type="number" min="0">
      <label>Hours</label><input id="h2" type="number" min="0">
      <label>Minutes</label><input id="m2" type="number" min="0">
      <label>Seconds</label><input id="s2" type="number" min="0">

      <label>Operation</label>
      <select id="operation">
        <option value="add">Add</option>
        <option value="subtract">Subtract</option>
      </select>

      <button onclick="calculateTime()">Calculate</button>
    </div>

    <div class="result" id="output"></div>
    
    <div class="section">
      <h2>🕰 Common Time Units</h2>
      <p>Time is measured using many units. While it can be added or subtracted like numbers, units vary in length. Here's a guide to help you understand:</p>
      <table style="width:100%; border-collapse: collapse; margin-top: 1rem;">
        <thead style="background:#f4f4f4;">
          <tr><th style="text-align:left; padding: 8px; border-bottom: 1px solid #ccc;">Unit</th><th style="text-align:left; padding: 8px; border-bottom: 1px solid #ccc;">Definition</th></tr>
        </thead>
        <tbody>
          <tr><td style="padding: 8px;">Millennium</td><td style="padding: 8px;">1,000 years</td></tr>
          <tr><td style="padding: 8px;">Century</td><td style="padding: 8px;">100 years</td></tr>
          <tr><td style="padding: 8px;">Decade</td><td style="padding: 8px;">10 years</td></tr>
          <tr><td style="padding: 8px;">Year</td><td style="padding: 8px;">365.242 days (avg)</td></tr>
          <tr><td style="padding: 8px;">Month</td><td style="padding: 8px;">28–31 days</td></tr>
          <tr><td style="padding: 8px;">Week</td><td style="padding: 8px;">7 days</td></tr>
          <tr><td style="padding: 8px;">Day</td><td style="padding: 8px;">24 hours / 1,440 min / 86,400 sec</td></tr>
          <tr><td style="padding: 8px;">Hour</td><td style="padding: 8px;">60 minutes or 3,600 seconds</td></tr>
          <tr><td style="padding: 8px;">Minute</td><td style="padding: 8px;">60 seconds</td></tr>
          <tr><td style="padding: 8px;">Second</td><td style="padding: 8px;">Base time unit</td></tr>
          <tr><td style="padding: 8px;">Millisecond</td><td style="padding: 8px;">1/1,000 of a second</td></tr>
        </tbody>
      </table>
    </div>
    <div class="back-link">
      <a href="/">← Back to All Calculators</a>
    </div>    
  </div>

  <script is:client="load">
    function calculateTime() {
      const d1 = parseInt(document.getElementById("d1").value) || 0;
      const h1 = parseInt(document.getElementById("h1").value) || 0;
      const m1 = parseInt(document.getElementById("m1").value) || 0;
      const s1 = parseInt(document.getElementById("s1").value) || 0;

      const d2 = parseInt(document.getElementById("d2").value) || 0;
      const h2 = parseInt(document.getElementById("h2").value) || 0;
      const m2 = parseInt(document.getElementById("m2").value) || 0;
      const s2 = parseInt(document.getElementById("s2").value) || 0;

      const op = document.getElementById("operation").value;

      let total1 = d1 * 86400 + h1 * 3600 + m1 * 60 + s1;
      let total2 = d2 * 86400 + h2 * 3600 + m2 * 60 + s2;

      let resultSeconds = op === "add" ? total1 + total2 : total1 - total2;
      if (resultSeconds < 0) resultSeconds = 0;

      const resD = Math.floor(resultSeconds / 86400);
      const resH = Math.floor((resultSeconds % 86400) / 3600);
      const resM = Math.floor((resultSeconds % 3600) / 60);
      const resS = resultSeconds % 60;

      const totalDays = resultSeconds / 86400;
      const totalHours = resultSeconds / 3600;
      const totalMinutes = resultSeconds / 60;

      document.getElementById("output").innerHTML = `
        <strong>Result:</strong><br>
        = ${resD} days ${resH} hours ${resM} minutes ${resS} seconds<br><br>
        = ${totalDays.toFixed(6)} days<br>
        = ${totalHours.toFixed(5)} hours<br>
        = ${totalMinutes.toFixed(3)} minutes<br>
        = ${resultSeconds.toLocaleString()} seconds
      `;
    }
  </script>
</body>
</html>
