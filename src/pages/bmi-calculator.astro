<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Body Weight Index Calculator (BMI) - Compute BMI Online Free</title>
    <meta name="description" content="Compute your BMI (Body Weight Index) using our free, easy-to-use calculator. Supports metric and imperial units. Fast, accurate, and modern." />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "What is the BMI calculator used for?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "A BMI calculator is used to estimate a person's body fat based on their height and weight. It's a quick way to assess if you're underweight, normal, overweight, or obese."
            }
          },
          {
            "@type": "Question",
            "name": "How accurate is the body weight index calculator?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "The BMI calculator provides a general health assessment. It’s a good starting point but doesn't account for muscle mass or body composition differences."
            }
          },
          {
            "@type": "Question",
            "name": "Can I use feet and inches instead of centimeters?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Yes! You can choose to enter height in either centimeters or in feet and inches for convenience."
            }
          }
        ]
      }
    </script>
    <style>
      body {
        font-family: 'Segoe UI', sans-serif;
        background-color: #f4f9f9;
        color: #333;
        margin: 0;
        padding: 0;
      }

      .container {
        max-width: 640px;
        margin: 4rem auto;
        padding: 2rem;
        background: #fff;
        border-radius: 1rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
      }

      h1 {
        text-align: center;
        margin-bottom: 1.5rem;
        color: #507D7D;
      }

      label {
        display: block;
        margin-top: 1rem;
        font-weight: 600;
      }

      input, select {
        width: 100%;
        padding: 0.7rem;
        margin-top: 0.25rem;
        border: 1px solid #ccc;
        border-radius: 0.6rem;
        font-size: 1rem;
      }

      .button-group {
        display: flex;
        justify-content: space-between;
        margin-top: 2rem;
      }

      button {
        flex: 1;
        margin: 0 0.5rem;
        padding: 0.8rem;
        border: none;
        border-radius: 2rem;
        background-color: #A8DADC;
        color: #1d3557;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.3s;
      }

      button:hover {
        background-color: #89C2D9;
      }

      .result {
        margin-top: 2rem;
        text-align: center;
        font-size: 1.2rem;
        color: #2a9d8f;
        font-weight: bold;
      }

      .interpretation {
        margin-top: 1rem;
        text-align: center;
        font-size: 1rem;
        color: #457b9d;
      }

      .faq {
        margin-top: 3rem;
        padding-top: 2rem;
        border-top: 1px solid #ddd;
      }

      .faq h2 {
        color: #264653;
        margin-bottom: 1rem;
      }

      .faq p {
        margin-bottom: 1rem;
      }

      .faq strong {
        display: block;
        margin-top: 1rem;
        color: #2a9d8f;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>Compute BMI – Body Weight Index Calculator</h1>

      <label for="unit">Unit</label>
      <select id="unit" onchange="toggleUnits()">
        <option value="metric">Metric (kg, cm)</option>
        <option value="imperial">Imperial (ft, in, lbs)</option>
      </select>

      <div id="metric-fields">
        <label for="heightCm">Height (cm)</label>
        <input type="number" id="heightCm" placeholder="e.g. 170" />

        <label for="weightKg">Weight (kg)</label>
        <input type="number" id="weightKg" placeholder="e.g. 65" />
      </div>

      <div id="imperial-fields" style="display: none;">
        <label for="heightFt">Height</label>
        <div style="display: flex; gap: 0.5rem;">
          <input type="number" id="heightFt" placeholder="feet" style="flex: 1;" />
          <input type="number" id="heightIn" placeholder="inches" style="flex: 1;" />
        </div>

        <label for="weightLb">Weight (lbs)</label>
        <input type="number" id="weightLb" placeholder="e.g. 150" />
      </div>

      <div class="button-group">
        <button onclick="calculateBMI()">Calculate</button>
        <button onclick="resetForm()">Reset</button>
      </div>

      <div class="result" id="bmiResult"></div>
      <div class="interpretation" id="bmiInterpretation"></div>

      <div class="faq">
        <h2>Frequently Asked Questions</h2>

        <p><strong>What is the BMI calculator used for?</strong>  
        It estimates your body fat using your height and weight. It's a quick way to assess your health range.</p>

        <p><strong>How accurate is the body weight index calculator?</strong>  
        It's a general guideline — not a full health assessment. It doesn't account for muscle mass.</p>

        <p><strong>Can I use feet and inches?</strong>  
        Yes! Choose imperial units to enter height in feet and inches, and weight in pounds.</p>
      </div>
    </div>

    <script>
      function toggleUnits() {
        const unit = document.getElementById("unit").value;
        document.getElementById("metric-fields").style.display = unit === "metric" ? "block" : "none";
        document.getElementById("imperial-fields").style.display = unit === "imperial" ? "block" : "none";
        resetForm();
      }

      function calculateBMI() {
        const unit = document.getElementById("unit").value;
        let bmi = 0;

        if (unit === "metric") {
          const heightCm = parseFloat(document.getElementById("heightCm").value);
          const weightKg = parseFloat(document.getElementById("weightKg").value);
          if (heightCm && weightKg) {
            const heightM = heightCm / 100;
            bmi = weightKg / (heightM * heightM);
          }
        } else {
          const heightFt = parseFloat(document.getElementById("heightFt").value);
          const heightIn = parseFloat(document.getElementById("heightIn").value);
          const weightLb = parseFloat(document.getElementById("weightLb").value);
          if (heightFt >= 0 && heightIn >= 0 && weightLb > 0) {
            const totalInches = (heightFt * 12) + heightIn;
            bmi = (weightLb / (totalInches * totalInches)) * 703;
          }
        }

        if (bmi > 0) {
          const result = bmi.toFixed(1);
          document.getElementById("bmiResult").textContent = `Your BMI is ${result}`;
          document.getElementById("bmiInterpretation").textContent = interpretBMI(bmi);
        } else {
          document.getElementById("bmiResult").textContent = "Please enter valid values.";
          document.getElementById("bmiInterpretation").textContent = "";
        }
      }

      function interpretBMI(bmi) {
        if (bmi < 18.5) return "You are underweight. Consider a nutrition plan.";
        else if (bmi < 24.9) return "You are in a healthy BMI range. Keep it up!";
        else if (bmi < 29.9) return "You are overweight. Consider some exercise.";
        else return "You are in the obese range. Please consult a healthcare provider.";
      }

      function resetForm() {
        document.querySelectorAll("input").forEach(i => i.value = "");
        document.getElementById("bmiResult").textContent = "";
        document.getElementById("bmiInterpretation").textContent = "";
      }
    </script>
  </body>
</html>
